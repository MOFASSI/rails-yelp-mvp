<h1>Hello we have those restaurants</h1>

  <%= link_to "Add new restaurant", "/restaurants/new" %>
<ul class="list-group">
  <% @restaurants.each do |restaurant| %>
    <li class="list-group-item"> <%= link_to restaurant.name , restaurant_path(restaurant) %>
      <p><strong>
      <%= link_to restaurant.category, restaurant_path(restaurant) %>
      </strong><p>

      </p>
     </li>
  <%end  %>
</ul>
